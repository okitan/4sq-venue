name: test

on:
  push:
    branches-ignore:
      - link-*
      - scrape-*

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm ci
      - run: npm test
